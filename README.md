# protein-fold-data
Data store for protein-fold-site — holds the live shared structure, energy score, and work-unit claims as state.json, read/written via the GitHub Contents API by the site’s Netlify Functions. Not meant to be deployed; just a JSON backing store.
